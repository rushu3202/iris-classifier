# Iris Classifier Project

This project trains a machine learning model on the Iris dataset and saves:
- Trained model (`outputs/iris_model.joblib`)
- Confusion matrix (`outputs/confusion_matrix.png`)

Run `python src/train.py` after installing dependencies.
git add outputs/
git commit -m "Add generated model and confusion matrix outputs"
git push


